# 个人博客

个人博客访问: https://luzhixing12345.github.io/

## 本地构建与预览

```shell
pip install zood
python build.py
```

脚本会将 `md-docs/` 构建到 `docs/`,在默认端口 `9381` 启动开发服务器并打开浏览器.
只构建静态文件可使用 `python build.py --build-only`.